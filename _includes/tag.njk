{%- if collections.tagList.indexOf(tag) != -1 -%}
	{% set tagUrl %}/tags/{{ tag }}/{% endset %}
	<a class="border border-gray-600 rounded-2xl px-2 py-1 text-sm bg-gray-100" href="{{ tagUrl }}">#{{ tag }}</a>
{% endif %}